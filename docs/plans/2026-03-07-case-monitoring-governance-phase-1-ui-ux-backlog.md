# 2026-03-07 Phase 1 UI/UX Backlog（案卷治理前端整改清单）

> 结论先说：**Phase 1 功能语义已对齐，不等于 UI/UX 已可用。**
> 当前问题主要不是“少几个按钮”，而是 **信息架构、页面分层、布局策略、文案质量、视觉层次** 同时失控。
> 本文件只做一件事：把现阶段已确认的 UI/UX 问题列成 backlog，后续逐条修，不再重复做现状审计。

---

## 0. 使用规则

- 这份 backlog 只记录 **UI/UX 与前端产品化问题**，不重复讨论 Phase 1 功能是否已实现。
- 后续修复时，**一次只处理一条或一组强相关条目**，避免再次把大堆 UI 改动混成一锅。
- 除非特别说明，默认目标是：
  - 让用户知道“我现在在哪”
  - 让用户知道“下一步该做什么”
  - 让页面内容能被完整看见
  - 让页面不是“静态表单拼盘”，而是“可操作的工作台”

---

## 1. P0：阻断级问题（必须先修）

- [x] **P0-01：修复页面中文乱码**
  - 状态：已完成（2026-03-07）
  - 现状：`task-detail.html`、`governance-session.html` 存在大面积乱码，页面已不可正常阅读。
  - 影响：这不是美观问题，而是直接不可用。
  - DoD：所有核心页面中文文案恢复正常，无乱码、无半截标签文本。
  - 落点：`apps/openagentic_sdk/priv/web/view/task-detail.html`、`apps/openagentic_sdk/priv/web/view/governance-session.html`、相关 JS 文案。

- [x] **P0-02：去掉会吃内容的固定视口/裁切布局**
  - 状态：已完成（2026-03-07）
  - 现状：全局 `.app { height: 100vh }`，多处 `overflow: hidden`，案卷页又是双栏卡片拼盘，内容一多就被 UI 吃掉。
  - 影响：用户看不全信息，滚动关系混乱，卡片内容容易被截断。
  - DoD：核心页面改成稳定的文档流/工作台滚动模型；长内容不会被裁切；主滚动区域唯一且可预期。
  - 落点：`apps/openagentic_sdk/priv/web/assets/app.css`、`apps/openagentic_sdk/priv/web/assets/case-governance.css`。

- [x] **P0-03：重做首页入口结构，去掉重复入口**
  - 状态：已完成（2026-03-07）
  - 现状：首页同时存在 `Cases` 和 `立此为案` 两个指向同一页的入口。
  - 影响：用户无法判断这是两个不同功能，还是一个功能的重复入口。
  - DoD：首页只保留清晰、不重复的主入口；“Workflow 主界面”和“案卷治理入口”关系明确。
  - 落点：`apps/openagentic_sdk/priv/web/index.html`。

- [x] **P0-04：明确产品主线，不再把“Workflow 主站”和“案卷治理”混成一页心智**
  - 状态：已完成（2026-03-07）
  - 现状：首页是 workflow 运行页，但顶部又硬塞案卷入口；进入案卷页后又像详情页、创建页、工作台混杂。
  - 影响：用户根本不知道当前在“跑流程”还是“管案卷”。
  - DoD：明确一级导航结构，至少区分：`Workflow`、`Cases`、`Inbox` 三类入口。
  - 落点：`apps/openagentic_sdk/priv/web/index.html`、`apps/openagentic_sdk/priv/web/view/cases.html`、全站 topbar。

---

## 2. P1：信息架构问题（优先级很高）

- [ ] **P1-01：拆分“案卷页”角色，不再让单页同时承担创建 / 定位 / 详情 / 模板 / 内邮**
  - 状态：进行中（2026-03-07）
  - 进度：`cases.html` 已拆出“案卷入口”和“案卷工作台”两层；创建案卷、进入既有案卷、工作台未激活空态已分开。治理页 / 任务详情页还未继续拆。
  - 现状：`cases.html` 同时塞了立案、定位、案卷总览、候选任务、正式任务、模板库、内邮。
  - 影响：页面像后台拼盘，不像产品页面；用户看不到主线。
  - DoD：明确 `案卷列表/入口页` 与 `案卷详情/工作台页` 的边界；至少把“创建案卷”和“处理既有案卷”区分开。
  - 落点：`apps/openagentic_sdk/priv/web/view/cases.html`、`apps/openagentic_sdk/priv/web/assets/case-governance.js`。

- [ ] **P1-02：为每个页面定义单一主任务**
  - 状态：进行中（2026-03-07）
  - 进度：`cases.html` 已明确当前主任务是“进入案卷”，并根据案卷状态给出唯一主 CTA；`governance-session.html`、`task-detail.html` 仍需继续收敛主任务。
  - 现状：治理页既有聊天、又有修订、又有补权、又有再激活；任务详情页又有版本、授权、运行、交付、补权。
  - 影响：没有“主操作”，全是并列模块。
  - DoD：每个页面只允许一个主任务，其它内容降级成辅助区或二级入口。
  - 建议：
    - 治理页主任务 = 治理对话 + 修订提交
    - 任务详情页主任务 = 看任务状态 + 看版本/授权结果
    - 补权/再激活可独立成次级流程区或抽屉

- [ ] **P1-03：补“我现在在哪”的层级导航**
  - 状态：进行中（2026-03-07）
  - 进度：`cases.html` 已补 `Cases / 当前案卷` breadcrumb；`task-detail.html`、`governance-session.html` 还未补到位。
  - 现状：页面缺 breadcrumb、缺对象标题、缺层级关系。
  - 影响：用户不知道当前对象是 case、candidate、task 还是 governance session。
  - DoD：页面顶部稳定出现对象层级，例如：`Cases / Case / Task / Governance Session`。
  - 落点：`cases.html`、`task-detail.html`、`governance-session.html`。

- [ ] **P1-04：补“下一步建议动作”而不是只堆模块**
  - 状态：进行中（2026-03-07）
  - 进度：`cases.html` 已按对象状态给出 CTA：`进入审议`、`先补权`、`查看任务详情`、`继续治理`、`重新抽取`；治理页 / 任务详情页还未补完。
  - 现状：页面告诉你有很多东西，但不告诉你当前应该先干什么。
  - 影响：用户只能自己猜主线。
  - DoD：根据对象状态给出明确 CTA，例如：
    - 候选待审 -> `进入审议`
    - 正式任务缺权 -> `先补权`
    - 修订后待重授权 -> `查看差异并补权`
    - 已就绪未激活 -> `激活任务`

---

## 3. P1：页面可读性与内容展示问题

- [ ] **P1-05：卡片内容不能再被固定高度与并列网格压缩**
  - 现状：双栏 + 多 panel + 最小高度 + 裁切，长文本和状态说明挤压严重。
  - DoD：长文本默认完整可读；卡片不因并列布局被压扁；必要时改成单栏纵向阅读流。

- [ ] **P1-06：减少“panelTitle + 空卡片”的后台感**
  - 现状：大量页面就是一堆 panel 标题 + 空态框，像接口调试台。
  - DoD：核心信息先以摘要卡/状态条呈现，次级信息再进入分区；空态不应占据页面主要视觉面积。

- [ ] **P1-07：统一空态文案和空态策略**
  - 现状：空态很多，但多数只是“暂无 xxx”，没有下一步。
  - DoD：空态必须带动作建议，如“先立案”“先进入治理”“先补授权”“还没有版本差异”。

- [ ] **P1-08：统一对象摘要卡格式**
  - 现状：candidate / task / mail / template 的卡片结构不统一，用户难以扫读。
  - DoD：统一字段顺序：标题 -> 状态 -> 关键摘要 -> 下一步动作 -> 辅助链接。

---

## 4. P1：交互与导航问题

- [ ] **P1-09：统一信箱入口不能只是裸按钮**
  - 现状：顶部只有 `统一信箱` 链接，没有未读数，没有当前状态提示。
  - DoD：顶部信箱入口至少显示未读计数；当前页可跳回上一级上下文。
  - 落点：全站 topbar、`/api/inbox/unread-count` 接入前端。

- [ ] **P1-10：治理页与任务详情页之间的跳转关系需要重构**
  - 现状：现在更像互相丢链接，不像一条稳定主线。
  - DoD：形成明确关系：`案卷 -> 任务详情 -> 进入治理` 或 `案卷 -> 进入治理 -> 查看详情`，不要双向随意乱跳。

- [ ] **P1-11：主页不应承担案卷创建快捷入口的主职责**
  - 现状：首页顶部的 `立此为案` 抢了产品结构的语义位置。
  - DoD：首页只负责 workflow；案卷创建应在 case 域内完成，或作为明确的二级入口。

---

## 5. P2：视觉与可访问性问题

- [ ] **P2-01：修复表单焦点态不统一的问题**
  - 现状：只有首页 composer 输入框有明确 focus 样式，case 页表单缺统一 focus 反馈。
  - DoD：所有 input / textarea / select / button 拥有一致可见的焦点态。
  - 落点：`app.css`、`case-governance.css`。

- [ ] **P2-02：减少全局 `transition: all`**
  - 现状：按钮、卡片等多处直接 `transition: all`。
  - 影响：容易造成不必要的动效、性能浪费和状态模糊。
  - DoD：只对必要属性做动画，例如 `background-color`、`border-color`、`box-shadow`、`transform`。

- [ ] **P2-03：统一按钮层级与语义色**
  - 现状：现在 `.btn` / `.btn.primary` / `.btn.danger` 只是基础区分，但主次按钮关系还不够清晰。
  - DoD：每个区域最多一个主按钮，其余为次按钮或文本按钮；危险操作色只用于真正危险动作。

- [ ] **P2-04：减少“技术说明直接糊在主界面上”的密度**
  - 现状：很多 hint 文本太长，制度说明直接塞在主流程里。
  - DoD：主流程只保留必要说明；长说明折叠到“查看制度说明/帮助”。

---

## 6. P2：产品语义问题

- [ ] **P2-05：统一命名，不要同一对象出现多套叫法**
  - 现状：case / 案卷 / 案宗，governance / 治理 / 审议，mail / 内邮 / 信箱，存在混用风险。
  - DoD：形成一份前端词汇表，并在页面文案中统一。

- [ ] **P2-06：页面标题与副标题要说“这是干嘛的”**
  - 现状：很多页面标题偏技术或偏抽象，不能快速传达用途。
  - DoD：标题、副标题要直接回答：这是哪个对象页、当前能做什么、下一步去哪。

---

## 7. 建议的整改顺序

按收益和返工风险，建议后续修复顺序如下：

1. `P0-01` 修乱码
2. `P0-02` 修滚动/裁切/视口策略
3. `P0-03` + `P0-04` 重做首页与一级导航
4. `P1-01` + `P1-02` 重构案卷页与治理页的信息架构
5. `P1-03` + `P1-04` 补层级导航与下一步 CTA
6. `P1-05` ~ `P1-08` 统一内容展示
7. `P1-09` ~ `P1-11` 修信箱与跨页导航
8. `P2-*` 做视觉、交互、文案打磨

---

## 8. 后续执行规则

后续如果你点名某条 backlog，我默认做以下事情：

- 只修该条及强相关项，不顺手改一大片
- 同步补前端/后端/测试中必要的最小闭环
- 修完后明确回写：
  - 改了哪些文件
  - 解决了哪条 backlog
  - 还剩哪些未动

